# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# This app

Read `/MOBILE_APP.md` (repo root) before working here — it has the phase
plan, the auth strategy, and the checklist to keep current in the same PR
as your change. `/PROJECT_SCOPE.md` has the backend API surface this app
consumes. There is no separate backend for mobile: every feature is a
client of the existing Next.js `/api/*` routes.
